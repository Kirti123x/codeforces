#include <bits/stdc++.h>
using namespace std;
#define int long long
typedef vector<int> vi;

void solve() {
    int h,n; cin >> h>>n;

    vi a(n), c(n);
    for(int i = 0; i<n; i++){
        cin>>a[i];
    }
    for(int i = 0; i<n; i++){
        cin>>c[i];
    }

    return;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        solve();
    }
}
